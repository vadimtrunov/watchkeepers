# Test fixture: an unknown top-level key. Strict-mode yaml.v3
# (KnownFields(true)) rejects this with a "field X not found in type Y"
# error which the loader chains through ErrUnknownField + ErrParseYAML.
keep:
  base_url: "http://example.com"
unexpected_top_level_key: "foo"
